# Deployment status

Production installation is intentionally out of scope until all five source phases pass. Phase 5 will provide systemd, dedicated-user, environment-file, Cloudflare Tunnel, upgrade, uninstall, backup, and installation documentation.

GitHub Actions must not deploy Server Agent or any managed project.
